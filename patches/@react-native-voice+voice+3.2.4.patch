diff --git a/node_modules/@react-native-voice/voice/android/build.gradle b/node_modules/@react-native-voice/voice/android/build.gradle
index 4a1d3a2..8a3c9b1 100644
--- a/node_modules/@react-native-voice/voice/android/build.gradle
+++ b/node_modules/@react-native-voice/voice/android/build.gradle
@@ -1,3 +1,18 @@
+// PATCH: Fix Gradle 8 implicit dependency issue for @react-native-voice/voice 3.2.4
+afterEvaluate {
+    def genDebug = tasks.findByPath(":react-native-voice_voice:generateDebugResValues")
+    def pkgDebug = tasks.findByPath(":react-native-voice:packageDebugResources")
+
+    if (genDebug != null && pkgDebug != null) {
+        println("[PATCH] Fixing Gradle task ordering for react-native-voice (Debug)")
+        pkgDebug.dependsOn(genDebug)
+    }
+
+    def genRelease = tasks.findByPath(":react-native-voice_voice:generateReleaseResValues")
+    def pkgRelease = tasks.findByPath(":react-native-voice:packageReleaseResources")
+
+    if (genRelease != null && pkgRelease != null) {
+        println("[PATCH] Fixing Gradle task ordering for react-native-voice (Release)")
+        pkgRelease.dependsOn(genRelease)
+    }
+}
